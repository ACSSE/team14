<phone:PhoneApplicationPage
    x:Class="Handy_Man_2._0.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    shell:SystemTray.IsVisible="True">

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- LOCALIZATION NOTE:
            To localize the displayed strings copy their values to appropriately named
            keys in the app's neutral language resource file (AppResources.resx) then
            replace the hard-coded text value between the attributes' quotation marks
            with the binding clause whose path points to that string name.

            For example:

                Text="{Binding Path=LocalizedResources.ApplicationTitle, Source={StaticResource LocalizedStrings}}"

            This binding points to the template's string resource named "ApplicationTitle".

            Adding supported languages in the Project Properties tab will create a
            new resx file per language that can carry the translated values of your
            UI strings. The binding in these examples will cause the value of the
            attributes to be drawn from the .resx file that matches the
            CurrentUICulture of the app at run time.
         -->

        <!--TitlePanel contains the name of the application and page title-->
        <StackPanel x:Name="TitlePanel" Grid.Row="0" Margin="12,17,0,28"/>

        <!--ContentPanel - place additional content here-->
        <Grid x:Name="ContentPanel" Margin="0,10,0,0" Background="#FFF3EEEE" Grid.RowSpan="2">
            <Grid HorizontalAlignment="Left" Height="125" Margin="10,0,0,0" VerticalAlignment="Top" Width="355" RenderTransformOrigin="0.433,0.479"/>
            <Grid Height="145" VerticalAlignment="Top" d:LayoutOverrides="LeftPosition, RightPosition">
                <TextBlock x:Name="textBlock" Margin="10,29,106,28" TextWrapping="Wrap" Text="Handy Man" d:LayoutOverrides="LeftPosition, RightPosition" Foreground="#FFF3C500" FontSize="65.333"/>
                <TextBlock x:Name="textBlock1" HorizontalAlignment="Right" Margin="0,29,0,28" TextWrapping="Wrap" Text="2.0" Width="101" Foreground="#FF01A185" FontSize="65.333"/>
            </Grid>
            <Grid Margin="0,130,0,140" Background="#FF01A185">
                <TextBlock x:Name="textBlock2" Margin="10,46,10,0" TextWrapping="Wrap" Text="Find trusted Man-Of-All-Work Online" VerticalAlignment="Top" FontSize="25.333"/>
                <TextBlock x:Name="textBlock3" HorizontalAlignment="Left" Margin="128,102,0,0" TextWrapping="Wrap" Text="Maintain Your Home" VerticalAlignment="Top" FontSize="22.667"/>
                <Button x:Name="btGetStarted" Content="Sign In" Margin="84,194,93,201" Background="#FFF3C500" BorderThickness="1,0,1,1" Click="btGetStarted_Click_1"/>
                <Image x:Name="image" Height="100" Margin="167,0,189,44" VerticalAlignment="Bottom" Source="/handyman-white.png"/>
            </Grid>
            <Grid Height="100" VerticalAlignment="Bottom" Background="#FF59595B">
                <TextBlock x:Name="textBlock4" Margin="130,25,208,29" TextWrapping="Wrap" Text="HandyMan" d:LayoutOverrides="LeftPosition, RightPosition" FontSize="22.667" Foreground="#FFF3C500"/>
                <TextBlock x:Name="textBlock5" Margin="0,25,177,48" TextWrapping="Wrap" Text="2.0" d:LayoutOverrides="Height" HorizontalAlignment="Right" Foreground="#FF01A185" FontSize="22.667"/>
                <TextBlock x:Name="textBlock6" Margin="10,0" TextWrapping="Wrap" Text="©2016 HandyMan 2.0. All Rights Reserved | Design " VerticalAlignment="Bottom" Height="29" FontSize="18.667"/>
            </Grid>
        </Grid>

        <!--Uncomment to see an alignment grid to help ensure your controls are
            aligned on common boundaries.  The image has a top margin of -32px to
            account for the System Tray. Set this to 0 (or remove the margin altogether)
            if the System Tray is hidden.

            Before shipping remove this XAML and the image itself.-->
        <!--<Image Source="/Assets/AlignmentGrid.png" VerticalAlignment="Top" Height="800" Width="480" Margin="0,-32,0,0" Grid.Row="0" Grid.RowSpan="2" IsHitTestVisible="False" />-->
    </Grid>

</phone:PhoneApplicationPage>